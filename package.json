{
  "name": "opencode-llm-wiki",
  "private": true,
  "version": "1.1.0",
  "description": "OpenCode LLM Wiki - Knowledge graph system with vector search",
  "scripts": {
    "dev": "cargo run --bin llm-wiki-server",
    "build": "cargo build --release",
    "test": "cargo test",
    "mcp": "cd src-mcp && npm start"
  }
}
